"""Helper modules for the methodology A/B benchmark (dev-time only).

Nothing here is imported by the production ranking path (rank.py / src/*).
These implement the alternative ranking methodologies the dataset's profiles
describe, so experiments/methodologies_ab.py can benchmark them on the local
labeled set.
"""
