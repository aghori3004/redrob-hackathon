# Sandbox app (app.py) — HuggingFace Spaces / local Gradio demo.
# Includes the reproduction deps plus Gradio.
-r requirements.txt
gradio>=5,<6   # 5.x: compatible with the huggingface_hub fastembed requires
